import ff from './fetch.fail';
import pt from './promise.throw';
import pto from './promise.timeout';
import localization from './localization';

export default {
  'fetch.fail': ff,
  'promise.throw': pt,
  'promise.timeout': pto,
  localization,
};
